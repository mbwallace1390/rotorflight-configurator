diff --git a/.github/workflows/pr.yml b/.github/workflows/pr.yml
index 6559ecf..5557ecd 100644
--- a/.github/workflows/pr.yml
+++ b/.github/workflows/pr.yml
@@ -176,7 +176,7 @@ jobs:
 
   macos-arm64:
 
-    runs-on: macos-latest
+    runs-on: macos-15
     needs: lint
 
     steps:
@@ -408,7 +408,7 @@ jobs:
         java-version: '17'
 
     - name: Setup Android SDK
-      uses: android-actions/setup-android@v3
+      uses: android-actions/setup-android@v4
       with:
         cmdline-tools-version: 13114758
         packages: 'tools platform-tools platforms;android-35 build-tools;35.0.0'
diff --git a/.github/workflows/push.yml b/.github/workflows/push.yml
index 089a2d6..f4050a1 100644
--- a/.github/workflows/push.yml
+++ b/.github/workflows/push.yml
@@ -168,7 +168,7 @@ jobs:
 
   macos-arm64:
 
-    runs-on: macos-latest
+    runs-on: macos-15
     needs: lint
 
     steps:
@@ -388,7 +388,7 @@ jobs:
         java-version: '17'
 
     - name: Setup Android SDK
-      uses: android-actions/setup-android@v3
+      uses: android-actions/setup-android@v4
       with:
         cmdline-tools-version: 13114758
         packages: 'tools platform-tools platforms;android-35 build-tools;35.0.0'
diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index 8b621db..e730ee0 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -125,7 +125,7 @@ jobs:
 
   macos-arm64:
 
-    runs-on: macos-latest
+    runs-on: macos-15
 
     steps:
     - name: Check out repository
@@ -341,7 +341,7 @@ jobs:
         java-version: '17'
 
     - name: Setup Android SDK
-      uses: android-actions/setup-android@v3
+      uses: android-actions/setup-android@v4
       with:
         cmdline-tools-version: 13114758
         packages: 'tools platform-tools platforms;android-35 build-tools;35.0.0'
diff --git a/.github/workflows/snapshot.yml b/.github/workflows/snapshot.yml
index f672190..676fdd8 100644
--- a/.github/workflows/snapshot.yml
+++ b/.github/workflows/snapshot.yml
@@ -125,7 +125,7 @@ jobs:
 
   macos-arm64:
 
-    runs-on: macos-latest
+    runs-on: macos-15
 
     steps:
     - name: Check out repository
@@ -341,7 +341,7 @@ jobs:
         java-version: '17'
 
     - name: Setup Android SDK
-      uses: android-actions/setup-android@v3
+      uses: android-actions/setup-android@v4
       with:
         cmdline-tools-version: 13114758
         packages: 'tools platform-tools platforms;android-35 build-tools;35.0.0'
diff --git a/cordova/config_template.xml b/cordova/config_template.xml
index 53e7957..465882c 100644
--- a/cordova/config_template.xml
+++ b/cordova/config_template.xml
@@ -13,11 +13,13 @@
     <allow-intent href="geo:*"/>
     <preference name="Orientation" value="default"/>
     <preference name="AutoHideSplashScreen" value="false"/>
+    <hook type="after_prepare" src="hooks/after_prepare/patch_android_pending_intents.js" />
 
     <platform name="android">
         <preference name="android-minSdkVersion" value="24" />
         <preference name="android-targetSdkVersion" value="35" />
         <preference name="android-compileSdkVersion" value="35" />
+        <resource-file src="lint.xml" target="app/lint.xml" />
 
         <edit-config file="/app/src/main/AndroidManifest.xml" mode="merge" target="/manifest/application">
             <application android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round"/>
diff --git a/cordova/hooks/after_prepare/patch_android_pending_intents.js b/cordova/hooks/after_prepare/patch_android_pending_intents.js
new file mode 100644
index 0000000..6ce97af
--- /dev/null
+++ b/cordova/hooks/after_prepare/patch_android_pending_intents.js
@@ -0,0 +1,70 @@
+/*
+ * Cordova hook: modernise PendingIntent calls in legacy USB plugins.
+ *
+ * Several USB plugins used by the Android build still create broadcast
+ * PendingIntents with a flags value of 0. Android 12+ requires an explicit
+ * mutability flag. UsbManager adds the permission result and device as extras,
+ * so these permission intents must remain mutable. The intent is also scoped
+ * to this app's package to satisfy Android 14+ restrictions on mutable,
+ * implicit PendingIntents.
+ */
+
+const fs = require("node:fs");
+const path = require("node:path");
+
+function walkJavaFiles(directory) {
+  if (!fs.existsSync(directory)) {
+    return [];
+  }
+
+  return fs.readdirSync(directory, { withFileTypes: true }).flatMap((entry) => {
+    const entryPath = path.join(directory, entry.name);
+    if (entry.isDirectory()) {
+      return walkJavaFiles(entryPath);
+    }
+    return entry.isFile() && entry.name.endsWith(".java") ? [entryPath] : [];
+  });
+}
+
+module.exports = function patchAndroidPendingIntents(context) {
+  const projectRoot = context?.opts?.projectRoot || process.cwd();
+  const javaRoot = path.join(
+    projectRoot,
+    "platforms",
+    "android",
+    "app",
+    "src",
+    "main",
+    "java",
+  );
+
+  let patchedCalls = 0;
+
+  for (const filePath of walkJavaFiles(javaRoot)) {
+    const original = fs.readFileSync(filePath, "utf8");
+
+    const updated = original.replace(
+      /PendingIntent\.getBroadcast\(\s*([^,\n]+)\s*,\s*([^,\n]+)\s*,\s*new Intent\(([^)\n]+)\)\s*,\s*0\s*\)/g,
+      (_match, contextExpression, requestCode, actionExpression) => {
+        patchedCalls += 1;
+        const androidContext = contextExpression.trim();
+        return `PendingIntent.getBroadcast(${androidContext}, ${requestCode.trim()}, new Intent(${actionExpression.trim()}).setPackage(${androidContext}.getPackageName()), PendingIntent.FLAG_MUTABLE)`;
+      },
+    );
+
+    if (updated !== original) {
+      fs.writeFileSync(filePath, updated, "utf8");
+      console.log(
+        `[android-pending-intent] Patched ${path.relative(projectRoot, filePath)}`,
+      );
+    }
+  }
+
+  if (patchedCalls === 0) {
+    console.log("[android-pending-intent] No legacy calls required patching.");
+  } else {
+    console.log(
+      `[android-pending-intent] Patched ${patchedCalls} legacy PendingIntent call(s).`,
+    );
+  }
+};
diff --git a/cordova/lint.xml b/cordova/lint.xml
new file mode 100644
index 0000000..74cb8aa
--- /dev/null
+++ b/cordova/lint.xml
@@ -0,0 +1,9 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<lint>
+    <!--
+      The application target is explicitly API 35 in config_template.xml.
+      This warning is emitted from legacy third-party Cordova metadata rather
+      than the generated application targetSdkVersion.
+    -->
+    <issue id="ExpiredTargetSdkVersion" severity="ignore" />
+</lint>
